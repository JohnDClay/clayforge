"""Packaged deploy templates for ClayForge.

These are real files (Dockerfile, railway.toml, etc.) intended for users to
copy or have the CLI write into their projects via `clayforge deploy --dir`.
"""
